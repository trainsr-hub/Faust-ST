{
  "formats": {
    "json": {
      "actions": ["read_all", "read_key", "overwrite", "backup"]
    },
    "jsonl": {
      "actions": ["append", "read_all", "clear", "backup"]
    },
    "sqlite": {
      "actions": ["read_all", "read_key", "upsert", "backup"]
    }
  }
}
